#include "main.h"

/**
 * print_alphabet - prints alphabets in lowercase
 *
 * Return:0 (Success)
 *
 */

	void print_alphabet(void)
	{
		char i;

		for (i = 'a'; i <= 'z'; i++)
		{
			putchar(i);
		}
		putchar('\n');
	}
	return (0);
